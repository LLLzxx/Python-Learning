# ------------未解决---------------
# PyQt模块中的一切窗口都是对象，需要用 class 来定义
import sys
# 导入需要的 PyQt库
from PyQt5 import QtWidgets,uic
# 加载在 Qt Designer 中创建的用户界面
# uic.loadUiType()方法返回的是一个列表，其中包含form_class 和 base_class 两个元素
# [0] 相当于 item[0] -- form_class
form_class = uic.loadUiType("My First Gui.ui")[0]
# 为主窗口定义一个类
class MyFirstWindow(QtWidgets.QMainWindow,form_class):
    def __init__(self, parent = None):
        QtWidgets.QMainWindow.__init__(self,parent)
        self.setupUi(self)
        #将 事件处理器 与 事件 关联起来
        # clicked事件--是button类可以触发的一个事件，另外还有 pressed（按下鼠标）事件 release（松开鼠标） 事件
        self.pushButton.clicked.connect(self.button_clicked)
    # 事件处理器
    # self 是指 当前方法所属的对象实例 -- 是包含用户界面组件的窗口对象
    # 此处事件是由背景或主窗口对象触发的 -- 这个窗口对象在调用事件处理器
    def button_clicked(self):
        x = self.pushButton.x()
        y = self.pushButton.y()
        x += 50
        y += 50
        self.pushButton.move(x,y)

# 运行事件循环的 PyQt 对象
app = QtWidgets.QApplication(sys.argv)
myWindow = MyFirstWindow()
# 启动程序并显示 GUI 窗口
myWindow.show()
app.exec_()
